all:
  hosts:
    palvelut-production:
      ansible_host: 203.0.113.10
      ansible_user: root
      deploy_authorized_key: "ssh-ed25519 REPLACE_WITH_DEPLOY_PUBLIC_KEY"
      # Supply palvelut_environment_file from an external or encrypted vars file.
      # Never commit production environment contents to this repository.
